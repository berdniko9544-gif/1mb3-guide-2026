# 🚀 Правильные шаги для деплоя на Vercel

## ⚠️ ВАЖНО: Это Next.js проект, НЕ Vite!

Когда Vercel спрашивает "Link to existing project?" - нужно ответить **N** (нет), чтобы создать НОВЫЙ проект.

---

## Правильная последовательность ответов:

1. **Set up and deploy?** → **Y** (да)

2. **Which scope?** → Выберите свой аккаунт (Enter)

3. **Link to existing project?** → **N** (НЕТ! Создать новый)

4. **What's your project's name?** → Введите имя или нажмите Enter (например: `1mb3-guide-2026`)

5. **In which directory is your code located?** → Нажмите Enter (текущая папка)

6. Vercel автоматически определит Next.js и начнёт сборку

---

## Если случайно выбрали Vite проект:

1. Нажмите **Ctrl+C** чтобы отменить
2. Удалите папку `.vercel`: `Remove-Item -Path ".vercel" -Recurse -Force`
3. Запустите заново: `vercel --prod`
4. На этот раз ответьте **N** на вопрос "Link to existing project?"

---

## После успешного деплоя:

Vercel выдаст ссылку типа: `https://your-project.vercel.app`

Сайт будет работать с:
- ✅ Всеми анимациями
- ✅ API routes (чат будет работать)
- ✅ Бесплатным SSL
- ✅ Автоматическими обновлениями при изменениях
