# 🧪 Как протестировать сайт локально

## ⚠️ Почему просто открыть index.html не работает?

Браузер блокирует загрузку CSS и JS файлов из папки `_next/` при открытии через `file://` из-за политики безопасности.

**Это нормально!** На реальном хостинге всё будет работать.

---

## ✅ Способ 1: Через npx (рекомендуется)

Откройте терминал в папке проекта и выполните:

```bash
npx serve out
```

Затем откройте в браузере: **http://localhost:3000**

---

## ✅ Способ 2: Через Python (если установлен)

```bash
cd out
python -m http.server 8000
```

Откройте: **http://localhost:8000**

---

## ✅ Способ 3: Через VS Code

1. Установите расширение "Live Server"
2. Правой кнопкой на `out/index.html`
3. "Open with Live Server"

---

## 🚀 На хостинге будет работать сразу!

Просто загрузите содержимое папки `out/` на хостинг через FTP или файловый менеджер.

**Никаких дополнительных настроек не требуется.**
