# 🌐 Деплой через веб-интерфейс Vercel (проще!)

## Способ 1: Через Git (рекомендуется)

### Шаг 1: Создайте Git репозиторий

```bash
git init
git add .
git commit -m "Initial commit"
```

### Шаг 2: Загрузите на GitHub

1. Создайте новый репозиторий на https://github.com/new
2. Назовите его, например: `1mb3-guide-2026`
3. Выполните команды:

```bash
git remote add origin https://github.com/ВАШ_USERNAME/1mb3-guide-2026.git
git branch -M main
git push -u origin main
```

### Шаг 3: Подключите к Vercel

1. Откройте https://vercel.com/new
2. Нажмите "Import Git Repository"
3. Выберите ваш репозиторий `1mb3-guide-2026`
4. Vercel автоматически определит Next.js
5. Нажмите "Deploy"

✅ Готово! Сайт будет доступен через 1-2 минуты.

---

## Способ 2: Прямая загрузка (без Git)

### Вариант A: Через Vercel CLI с принудительным созданием нового проекта

```bash
# Удалите .vercel если есть
Remove-Item -Path ".vercel" -Recurse -Force -ErrorAction SilentlyContinue

# Деплой с автоматическим созданием нового проекта
vercel --prod --force
```

При вопросах отвечайте:
- Set up and deploy? → **Y**
- Which scope? → **Enter**
- Link to existing project? → **N** (обязательно N!)
- Project name? → `1mb3-guide-2026` или Enter
- Directory? → **Enter**

---

## Способ 3: Через архив на Vercel Dashboard

1. Откройте https://vercel.com/new
2. Выберите "Browse" или перетащите папку проекта
3. Загрузите всю папку `d:/1mb3_INVESTABLE_UX_2026 (1)`
4. Vercel определит Next.js и задеплоит

---

## Что выбрать?

- **Git + GitHub** - лучший вариант для автоматических обновлений
- **CLI с --force** - если хотите через терминал
- **Веб-интерфейс** - самый простой для первого раза

---

## После деплоя

Ваш сайт будет доступен по адресу:
`https://1mb3-guide-2026.vercel.app` (или другое имя)

Можно подключить свой домен в настройках проекта.
